"onhover": {
    "enable": false, // Disable hover interaction
    "mode": "repulse"
}
"interactivity": {
    "events": {
        "onhover": {
            "enable": false // Disable hover interaction
        },
        "onclick": {
            "enable": false // Disable click interaction
        }
    }
}
